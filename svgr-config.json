{
  "typescript": false,
  "removeViewBox": false,
  "replaceAttrValues": {
    "white": "{props.color || `#FFFFFF`}",
    "#FFFFFF": "{props.color || `#FFFFFF`}",
    "black": "{props.color || `currentColor`}",
    "#111111": "{props.color || `currentColor`}",
    "#000": "{props.color || `currentColor`}"
  },
  "svgProps": {
    "width": "{props.width || 256}",
    "height": "{props.height || 256}"
  }
}
