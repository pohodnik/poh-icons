{
  "name": "@pohodnik/icons",
  "version": "1.0.4",
  "description": "Icons for pohodnik webasites",
  "main": "svg/index.js",
  "repository": "git@github.com:pohodnik/poh-icons.git",
  "author": "pohodnik <pohodnik58@gmail.com>",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@svgr/cli": "^6.5.1"
  },
  "scripts": {
    "patch": "npm version patch && git push && npm publish",
    "icons:create": "npx @svgr/cli -d ./svg ./src --config-file ./svgr-config.json --template ./svg-template.js --svgo-config ./svgo-config.json"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "directories": {
    "lib": "./svg"
  }
}
